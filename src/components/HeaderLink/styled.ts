import styled from "@emotion/styled";

export const Link = styled.a`
  height: 100%;
  color: #fff;
  display: block;
  & > svg {
    font-size: 32px;
  }
  & > img {
    height: 100%;
    display: block;
  }
`;
