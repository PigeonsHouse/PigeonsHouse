export type WorkData = {
  name: string;
  thumbnail: string;
  genre: string;
  description: string;
  url: string | null;
  download_url: string | null;
  source_url: string | null;
  created_at: string;
};
